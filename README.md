# Layer: Layer and Layerset Build

Kaptain config layer providing common build settings for layer and layerset projects.

Projects that reference this layer inherit:

- **Multi-arch docker builds** — linux/amd64 and linux/arm64
- **2-part versioning** — major.minor only
